{
  "framework": "vite",
  "buildCommand": "cd artifacts/downtown-fitness && pnpm run build",
  "outputDirectory": "artifacts/downtown-fitness/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
